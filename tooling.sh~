# source me!!

# i.e. run:

# . bootstrap.sh

# or

# source bootstrap.sh

# figure out which directory is you anaconda dir
export ANACONDA_DIR=$(dirname $(dirname $(which conda)))

# install invoke here
$ANACONDA_DIR/bin/pip install invoke
